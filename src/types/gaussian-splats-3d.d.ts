// No official/community type definitions exist for this package. Used
// only via a dynamic `import()` treated as `any` in splat-viewer.tsx —
// this ambient declaration just satisfies TypeScript's module resolution.
declare module "@mkkellogg/gaussian-splats-3d";
